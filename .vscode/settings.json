{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:pwort}/passwords.db",
            "name": "pwort"
        }
    ]
}
